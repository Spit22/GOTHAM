version: '3.9'
services:
  orchestrator:
    image: orchestrator_img:latest
    #volumes: 
    #    - install_orchestrator.sh:~/GOTHAM/Install/
    restart: always
    environment:
      MARIADB_ROOT_PASSWORD: root